<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # 301 Redirect /index.php to https://drsujitchowdhary.com/
    RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\ /index\.php\ HTTP/
    RewriteRule ^index\.php$ https://drsujitchowdhary.com/ [R=301,L]

    # 301 Redirect deleted undescended testis service to homepage
    RewriteRule ^service/undescended-testis(\.php)?$ / [R=301,L]
    RewriteRule ^service-undescended-testis(\.(php|html))?$ / [R=301,L]

    # 301 Redirect old service-name.php, service-name.html, or service-name to /service/name.php
    RewriteRule ^service-([a-z0-9-]+)(\.(php|html))?$ /service/$1.php [R=301,L]

    # 301 Redirect any other .html requests to .php equivalent
    RewriteCond %{THE_REQUEST} \s/+(.+)\.html[\s?] [NC]
    RewriteRule ^ /%1.php [R=301,L]

    # Allow extensionless URLs to serve .php files
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME}\.php -f
    RewriteRule ^(.*)$ $1.php [L]
</IfModule>

# Disable browser caching for dynamic PHP pages
<IfModule mod_headers.c>
    <FilesMatch "\.(php|html|htm)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>
</IfModule>
